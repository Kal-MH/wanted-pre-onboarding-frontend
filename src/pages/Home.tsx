import styled from "@emotion/styled";

const Home = () => {
  return (
    <Container>
      <h1>Welcome!!</h1>
    </Container>
  );
};

export default Home;

const Container = styled.div`
  display: flex;
  justify-content: center;
  align-items: center;
`;
